<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.PopupTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
    <style name="AppTheme.DarkElementTheme">
        <item name="android:textColor">@color/darkModeText</item>
        <item name="android:background">@color/darkModeBackground</item>
    </style>
    <style name="AppTheme.DarkHeaderTheme">
        <item name="android:textColor">@color/darkModePrimary</item>
        <item name="android:background">@color/darkModeSecondary</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="AppTheme.HeaderTheme">
        <item name="android:textColor">#000000</item>
        <item name="android:background">#CCCCCC</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
